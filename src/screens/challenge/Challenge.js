import { View, Text } from "react-native";

/**
 * @author VAMPETA
 * @brief TELA DE DESAFIO
*/
export default function Challenge({ navigation }) {

	return (
		<View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
			<Text style={{ color: "white" }} >Desafio</Text>
		</View>
	);
}