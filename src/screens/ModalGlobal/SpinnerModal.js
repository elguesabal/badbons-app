import { ActivityIndicator } from "react-native";

/**
 * @author VAMPETA
 * @brief COMPONENTE SPINNER DA TELA DO ModalGlobal
*/
export default function SpinnerModal() {
	return (
		<ActivityIndicator size="large" color="white" />
	);
}